Suggest the next feature to work on.

1. Run `./plan/flow list --limit 10` to get the top candidates
2. Suggest one with a short reason — what it unlocks, why now, what it depends on
3. Run `./plan/flow feature <slug>` on the suggested feature and show the output
4. Wait for the user to confirm or redirect
